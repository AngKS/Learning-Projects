

// get body tag
const body = document.querySelector('body');

// add a box element to the body
const elem = `<div class="box"></div>`;

// add a box element to the body
body.insertAdjacentHTML('beforeend', elem);

 

